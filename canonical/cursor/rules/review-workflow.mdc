---
description: Cross-model review workflow. Apply when a session is invoked as `review <task-id>` or asked to review a dev session's work on a task. Does not apply to `task <id>` development.
alwaysApply: false
---

# Cross-model review workflow (Cursor pointer)

The workflow text is single-sourced. Read and follow the canonical document:
`.ai-protocol/protocols/review.md` (the review contract, gitignored with the
rest of the deployed `.ai-protocol/` suite).

This pointer exists so the rule loads on the `review <id>` verb; all content —
procedure, review-entry shape (`Verdict:` / `Group:` / `Findings:`), and the
convergence rules (findings ledger, convergence groups) — lives in the
canonical file. Do not duplicate it here.
