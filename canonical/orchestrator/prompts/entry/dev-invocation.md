You are invoked as: `task {{task_id}}` — dev role per the loader's verb→contract mapping. Develop or continue the task per the dev contract (`.ai-protocol/protocols/dev.md`, in your protocol context). {{sid_line}} If the session log contains review entries with unresolved findings, verify each finding against the actual code first, fix the valid ones (correctness first), and record any finding you verify as invalid in your session-log entry as a dispute — do not silently fix or silently skip it.
