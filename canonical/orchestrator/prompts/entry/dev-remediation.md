REMEDIATION SESSION (roles §11, remediation before advancement): the latest review verdict is changes-requested, so this session ONLY remediates that review — fix the valid findings, record disputes for invalid ones, then hand back to review. Do NOT advance new scope (no new plan steps or features) this session; new scope resumes after a pass verdict. Do NOT run preReEst; if useful, write `Plan-slice: remediation for review group {{group}}` in the session-log entry.
