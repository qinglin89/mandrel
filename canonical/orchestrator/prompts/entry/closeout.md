Task '.ai-tasks/{{task_id}}.md' has status: completed. Invoke /ai-sync-v2 now — read '{{sync_skill}}' and follow it — to apply absorption and archive the task. Before ending, audit all {{active_count}} remaining active task file(s) in `.ai-tasks/` excluding `archive/`: update any blocker, scope, assumptions, acceptance criteria, prefetch, estimate, or status changed by this completed task. Your final response must include one line beginning `Remaining-task audit:` summarizing how many active tasks you checked and which task ids changed or did not change. End with a clean working tree.
