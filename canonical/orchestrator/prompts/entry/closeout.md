Task '.ai-tasks/{{task_id}}.md' has status: completed. Invoke /ai-sync-v2 now — read '{{sync_skill}}' and follow it in full — to run the task-completion closeout ({{active_count}} other active task file(s) to reconcile). Your final response must include one line beginning `Remaining-task audit:`; end with a clean working tree.
