[orchestrator] The human answered your blocker: {{answer}}
Restore `status` to its pre-blocked value (the status on the left of the `→ blocked` in your last session-log entry heading), clear `blockers`, and continue the session per protocol (AUTOMATION MODE rules still apply; end with a clean tree + session-log entry).
