# Claude Project Entry

Use `AGENTS.md` as the canonical instruction file.
For project memory, start from `.ai/index.md`.

## Claude Code Specific

knowledge that would be valuable in future sessions. Skip if not.
Before running /compact, check and update .ai/ if the session produced

Before running /compact or when approaching context limits,
check and update .ai/ if the session produced knowledge that
would be valuable in future sessions. Skip if not.
