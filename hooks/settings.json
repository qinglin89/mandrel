{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "CURRENT=$(.ai/scripts/sync-hash.sh); [ -z \"$CURRENT\" ] && exit 0; SAVED=$(cat .ai/.sync-hash 2>/dev/null || echo ''); [ \"$CURRENT\" = \"$SAVED\" ] && exit 0; echo '.ai/ sync required: code changed (.go/.proto/.yaml) since last sync. Run /ai-sync skill now.' >&2; exit 2"
          }
        ]
      }
    ]
  }
}
