# Evolution experiments

Each experiment records a baseline revision, candidate revision, frozen cohort,
evaluator/rubric revision, expected directional changes, observed outcomes,
regressions, and the human promote/revise/revert decision.

An implementation task is not evidence of improvement; promotion depends on a
completed replay or canary record.
