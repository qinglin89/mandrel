# Curated evolution cases

Store only sanitized, reusable regression cases admitted from evaluation
evidence. Raw task content and report bundles remain under ignored
`.ai-evolution/imported-artifacts/`.

Every case identifies its source report hashes, redactions, applicable
protocol/evaluator revisions, and the behavior it is intended to test.
