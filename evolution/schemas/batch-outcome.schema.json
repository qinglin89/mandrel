{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ai-native-development.local/evolution/batch-outcome.schema.json",
  "title": "Evolution batch outcome",
  "description": "The terminal record of one batch. Its presence is what ends the batch and releases the next cohort (contract invariant 14), which is why it is committed: `.ai-evolution/` is machine-local, so a batch whose conclusion lived only in runtime state would read as current on every other clone forever.",
  "type": "object",
  "required": [
    "schema_version",
    "batch_id",
    "outcome",
    "decided_at",
    "reason",
    "experiment_id",
    "promotion_revision"
  ],
  "properties": {
    "schema_version": {
      "const": 1
    },
    "batch_id": {
      "type": "string",
      "pattern": "^evolution-batch-[0-9]{4,}$"
    },
    "outcome": {
      "description": "`promoted` names the experiment whose candidate reached the source line. `no-change` is the valid conclusion of invariant 7 and fabricates nothing: with it, `experiment_id` and `promotion_revision` are both null, and no candidate, merge, deployment, or revision change is invented to represent a change that was not made. The controller enforces that pairing; the implemented JSON Schema subset cannot express it.",
      "enum": [
        "promoted",
        "no-change"
      ]
    },
    "decided_at": {
      "description": "When the human decision was recorded, not when the analysis or the last round concluded.",
      "type": "string",
      "format": "date-time"
    },
    "reason": {
      "type": "string",
      "minLength": 1
    },
    "experiment_id": {
      "type": [
        "string",
        "null"
      ],
      "pattern": "^evolution-batch-[0-9]{4,}-exp-[0-9]{2,}$"
    },
    "promotion_revision": {
      "description": "The source-line commit carrying the promoted change — distinct from the candidate tip measured, and from the effective revision each target reaches only when redeployed.",
      "type": [
        "string",
        "null"
      ],
      "pattern": "^[0-9a-f]{40}$"
    }
  },
  "additionalProperties": false
}
